SPOTCLI

A command line toool to play your spotify playlists, no need for a spotify player to be opened. This projext uses librespot as a player.

to start simply run .setp.sh once, from there on either build and run by buildAndRun.sh or jst build and then start the executable in build/spotcli
